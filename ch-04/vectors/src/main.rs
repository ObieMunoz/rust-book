fn main() {
    let v: Vec<i32> = vec![1, 2, 3]; // vector with 1, 2, 3
    println!("{:?}", v);

    let v: Vec<i32> = vec![3; 10]; // vector with 10 indices all containing 3
    println!("{:?}", v);
}
